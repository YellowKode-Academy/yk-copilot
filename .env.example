# Proxy port — serves the proxy API and dashboard at http://localhost:9999
PROXY_PORT=9999

# Ollama endpoint — by default runs inside Docker (works on all platforms)
# For GPU acceleration on Mac/Windows: install Ollama natively from https://ollama.com
# then pull models manually and uncomment the line below:
# OLLAMA_API_URL=http://host.docker.internal:11434

# Playwright MCP (leave as-is when using docker compose)
PLAYWRIGHT_MCP_URL=http://yk_playwright:8931

# Fast model: used for simple / short requests
MODEL_FAST=qwen2.5-coder:7b

# Smart model: used for complex / multi-turn / tool-heavy requests
MODEL_SMART=qwen2.5-coder:14b

# Vision model: used to describe images before passing context to the code model
MODEL_VISION=gemma4:e4b

# ── Claude Code setup ──────────────────────────────────────────────────────────
# After starting docker compose, set these in your terminal and run claude:
#
#   export ANTHROPIC_BASE_URL=http://localhost:9999
#   export ANTHROPIC_API_KEY=ollama
#   claude
#
# Or just run: yk-copilot on  (after sourcing scripts/yk-switch.sh)
